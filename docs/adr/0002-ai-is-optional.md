# ADR 0002: AI is optional

Accepted for v0.1. Local diagnostics must work without credentials, network access or provider availability. Providers sit outside core behind a typed contract. A single structured pass can elaborate or abstain within the deterministic evidence envelope. Invalid or failed requests restore the baseline and emit a warning. Default CI uses fakes; live smoke is explicit.
