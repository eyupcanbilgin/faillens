# ADR 0004: Defer MCP

Accepted for v0.1. Prove that local failure triage is useful before adding a protocol server. The framework-independent evidence model and read-only provider contract permit a future bounded interface. v0.2 proposals are documented; no MCP runtime, shell or fixing tools are shipped now.
